# Map Migration — Complete

Migration from `@vnedyalk0v/react19-simple-maps` (SVG) to `react-leaflet` (Leaflet) is **complete** as of 2026-03-02.

Leaflet is the sole map implementation. All legacy SVG code, the variant switching logic, and the `@vnedyalk0v/react19-simple-maps` dependency have been removed.
