# Rahal Frontend

## Run Locally

```bash
cd frontend
npm install
npm run dev
```

Notes:
- Use `npm` in this directory as the package manager for frontend.
- Running `pnpm dev` from repo root may start a different app and cause dependency resolution issues.
